/** @type {import('tailwindcss').Config} */
export default {
  content: [
    "./index.html",
    "./src/**/*.{js,ts,jsx,tsx}",
  ],
  theme: {
    extend: {
      fontFamily: {
        // Sử dụng Inter như đã hướng dẫn
        sans: ['Inter', 'sans-serif'],
      },
      // Thêm các keyframe hoặc màu sắc tùy chỉnh nếu cần
    },
  },
  plugins: [],
}